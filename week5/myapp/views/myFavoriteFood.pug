doctype doctype html 
html(lang='en')
    head 
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        -var auther = "Tom Chu"
        h1 #{title} #{auther}
        h2= auther
        h2 #{auther}
        .fruits 
            ul 
                each   val, key in fruits
                    li #{key} --- #{val}
